//  ************************ React Hooks *********************

// React hooks is the alternative approach of state management and lifecycle methdos. There are many hooks used for different purposes. Some of them are useReducer, useState, useCallack, useMemo, useRef, useDispatcher etc 

// Componenet Life Cycle

// Initialization :- This is the stage where the componenet is constructed with the given props and default state. This is done in the constructor ofa componenet class 
// Mounting:- Mounting is the stage of rendering the JSX returned by the render method itself
// Updating :- Updating is the stage when the state of a component is updated and the application is repainted
// Unmounting :- As the name suggest Unmounting is the final step of the component lifecycle where the component is removed from the page  


